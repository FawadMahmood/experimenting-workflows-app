{
  "name": "experimenting-workflow",
  "version": "1.0.0",
  "description": "Building new workflow file",
  "main": "index.js",
  "scripts": {
    "start": "probot run ./index.js",
    "dev": "smee -u $WEBHOOK_PROXY_URL -t http://localhost:3000/api/github/webhooks & probot run ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "probot": "^14.2.4"
  },
  "devDependencies": {
    "smee-client": "^5.0.0"
  }
}
